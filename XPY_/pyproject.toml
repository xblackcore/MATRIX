[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "."}

[project]
name = "XPY"
version = "0.1.0"
description = "META-Python"
authors = [
    { name = "Sir. Doyle; S-Class Astartes at BlackCoreΔ" }
]
requires-python = ">=3.8"
dependencies = []